configuration:
  git_url:
    name: Git Repository URL
    description: Hermes Agent git repository URL.
  git_ref:
    name: Git Branch/Tag
    description: >-
      Branch, tag, or commit hash to check out.
      Leave empty for the repository's default branch.
  git_token:
    name: Git/GitHub Token
    description: >-
      Personal access token for private repositories (optional).
      Also exported as GITHUB_TOKEN for gh CLI and Hermes skills.
  auto_update:
    name: Auto Update
    description: >-
      Pull latest changes from git on every restart.
  hass_url:
    name: Home Assistant URL
    description: Home Assistant base URL for API access.
  homeassistant_token:
    name: Home Assistant Token
    description: Long-lived access token for HA API integration (optional).
  enable_dashboard:
    name: Enable Web Dashboard
    description: >-
      ⚠️ Enable web dashboard on direct HTTP/HTTPS ports (configured under Network below).
      Set an access password to secure these ports.
  enable_terminal:
    name: Enable Web Terminal
    description: >-
      ⚠️ Enable web terminal on direct HTTP/HTTPS ports (configured under Network below).
      Set an access password to secure these ports.
  enable_api:
    name: Enable API Server
    description: >-
      ⚠️ Enable the OpenAI-compatible API server on direct HTTP/HTTPS ports (configured under Network below).
      The access password doubles as the server API key. Surrounding whitespace is ignored;
      the remaining value must contain at least 16 printable ASCII characters. Common placeholder
      values, single-quote and backslash characters, dotenv interpolation syntax, and line breaks are rejected.
  enable_desktop_backend:
    name: Enable Hermes Desktop Backend
    description: >-
      ⚠️ Opt in to the powerful Hermes Desktop remote backend on container port 9119.
      Requires an access password. Map the port under Network and expose it only on a trusted LAN/VPN/Tailscale path.
      Authenticated Desktop users receive full agent control and accept that risk.
  access_password:
    name: Access Password
    description: >-
      ⚠️ Password for direct HTTP/HTTPS access. Username: hermes.
      Also used as the server API key and Hermes Desktop password. Line breaks are rejected in every mode.
      When the API server is enabled, surrounding whitespace is ignored and the remaining value must
      contain at least 16 printable ASCII characters, cannot be a common placeholder, and cannot
      contain a single-quote or backslash character or dotenv interpolation syntax.
  env_vars:
    name: Hermes .env Variables
    description: >-
      Written to each resolved profile's .env on each start.
      Names must be standard shell-variable identifiers and values must be single-line.
      Use for API keys (e.g. OPENROUTER_API_KEY).
      Non-empty values override existing .env entries.
  hermes_home:
    name: Hermes Home Directory
    description: >-
      Single-profile mode: directory name for Hermes Agent data (relative to /config).
      Default ".hermes". Change to e.g. "amy" for /config/amy/. Ignored when
      the "profiles" list below is non-empty.
  profiles:
    name: Profiles (multi-profile mode)
    description: >-
      List of profile directories (relative to /config) to run concurrently.
      Bare names are placed under "profiles_base" (default .hermes/profiles).
      Non-dotted entries containing "/" are also placed under "profiles_base".
      Entries starting with "." are used as-is.
      The first entry is the primary and keeps the root routes (/hermes/,
      /dashboard/, /v1/, /terminal/). Each additional profile is served under
      /profile/<name>/...
  profiles_base:
    name: Profiles Base Directory
    description: >-
      Default parent directory for bare profile names (default ".hermes/profiles",
      matching upstream Hermes' convention). Set to empty to disable the prefix
      and keep all profile dirs directly under /config/.
  profile_env_vars:
    name: Per-profile .env Overrides
    description: >-
      Per-profile overrides layered on top of the shared "env_vars" above.
      "profile" must match a directory in the "profiles" list exactly.
      Repeat the entry for each variable on the same profile.
