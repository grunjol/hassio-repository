name: Hermes Agent
version: "1.3.1.1"
slug: hermes-agent
image: ghcr.io/grunjol/addon-hermes-agent
description: "The self-improving AI agent built by Nous Research. Home Assistant add-on by Wolfram Ravenwolf, fork by grunjol with pre-built images and container fixes."
url: "https://github.com/grunjol/addon-hermes-agent"
arch:
  - amd64
  - aarch64
init: false
startup: application
boot: auto
ingress: true
ingress_port: 49169
ingress_stream: true
panel_icon: mdi:robot
panel_title: Hermes Agent
map:
  - addon_config:rw
  - media:rw
  - share:rw
ports:
  8080/tcp: null
  8443/tcp: 8443
  9119/tcp: null
ports_description:
  8080/tcp: "HTTP: /hermes/ (agent) · /dashboard/ (web UI) · /terminal/ (shell) · /v1/ (OpenAI API)"
  8443/tcp: "HTTPS: /hermes/ (agent) · /dashboard/ (web UI) · /terminal/ (shell) · /v1/ (OpenAI API) — TLS (self-signed cert)"
  9119/tcp: "Hermes Desktop remote backend — opt-in; trusted LAN/VPN/Tailscale only"
options:
  git_url: "https://github.com/NousResearch/hermes-agent.git"
  git_ref: ""
  git_token: ""
  auto_update: false
  hass_url: "http://homeassistant.local:8123"
  homeassistant_token: ""
  hermes_home: ".hermes"
  profiles: []
  profiles_base: ".hermes/profiles"
  profile_env_vars: []
  enable_dashboard: false
  enable_terminal: false
  enable_api: false
  enable_desktop_backend: false
  access_password: ""
  apply_fixes: true
  env_vars:
    - name: "OPENROUTER_API_KEY"
      value: ""
schema:
  git_url: "url?"
  git_ref: "str?"
  git_token: "password?"
  auto_update: "bool"
  hass_url: "url?"
  homeassistant_token: "password?"
  hermes_home: "str?"
  profiles:
    - "str"
  profiles_base: "str?"
  profile_env_vars:
    - profile: "str"
      name: "match(^[A-Z_][A-Z0-9_]{0,254}$)"
      value: "str"
  enable_dashboard: "bool"
  enable_terminal: "bool"
  enable_api: "bool"
  enable_desktop_backend: "bool"
  access_password: "password?"
  apply_fixes: "bool"
  env_vars:
    - name: "match(^[A-Z_][A-Z0-9_]{0,254}$)"
      value: "str"
